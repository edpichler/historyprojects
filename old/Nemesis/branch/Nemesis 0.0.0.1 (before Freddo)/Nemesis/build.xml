<?xml version="1.0" encoding="windows-1252" ?>
<!--Ant buildfile generated by Oracle JDeveloper 10g-->
<!--Generated 28/04/2006 21:19:48-->
<project name="Nemesis" default="all" basedir=".">
  <property file="build.properties"/>
  <buildnumber id="buildNumber" />

  <fileset id="AllLibs" dir="${lib.dir}" includes="*.jar">        
    
  </fileset>
  <fileset id="EvilFramework" dir="/D:/projetos_do_mal/EvilFramework/deploy/">
    <filename name="EvilFramework.jar"/>
  </fileset>
  <path id="classpath">
    <fileset refid="AllLibs"/>    
    <fileset refid="EvilFramework"/>    
  </path>
  
  <target name="init">
    <tstamp/>
    <mkdir dir="${output.dir}"/>
    <mkdir dir="${deploy.dir}"/>
  </target>
  
  <target name="all" description="Build the project" depends="compile,copy"/>
  <target name="clean" description="Clean the project">
    <delete includeemptydirs="true" quiet="true">
      <fileset dir="${output.dir}" includes="**/*"/>
      <fileset dir="${deploy.dir}" includes="**/*"/>
    </delete>
  </target>
  <target name="compile" description="Compile Java source files" depends="init">
    <javac destdir="${output.dir}" classpathref="classpath"
           debug="${javac.debug}" nowarn="${javac.nowarn}"
           deprecation="${javac.deprecation}" encoding="Cp1252" source="1.5"
           target="1.5">
      <src path="src"/>
    </javac>
  </target>
  <target name="copy" description="Copy files to output directory"
          depends="init">
    <patternset id="copy.patterns">
      <include name="**/*.gif"/>
      <include name="**/*.jpg"/>
      <include name="**/*.jpeg"/>
      <include name="**/*.png"/>
      <include name="**/*.properties"/>
      <include name="**/*.xml"/>
      <include name="**/*-apf.xml"/>
      <include name="**/*.ejx"/>
      <include name="**/*.xcfg"/>
      <include name="**/*.cpx"/>
      <include name="**/*.dcx"/>
      <include name="**/*.wsdl"/>
      <include name="**/*.ini"/>
      <include name="**/*.tld"/>
      <include name="**/*.tag"/>
    </patternset>
    <copy todir="${output.dir}">
      <fileset dir="src">
        <patternset refid="copy.patterns"/>
      </fileset>
    </copy>
  </target>
  
  <target name="buildJar" description="uhull" depends="all">
    <jar basedir="${output.dir}" compress="yes" keepcompression="yes" 
	jarfile="${deploy.dir}/Nemesis.jar" filesetmanifest="merge"/>
  </target>
</project>
